# src

This directory will house the modularized source code for the Essence Engine simulation. For now it only contains the compatibility entry point in `index.js`, but future agents should migrate the legacy scripts here in a structured way.
